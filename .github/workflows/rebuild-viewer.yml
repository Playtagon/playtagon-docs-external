name: Rebuild Docs Viewer

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  rebuild-viewer:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl -fsS -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
